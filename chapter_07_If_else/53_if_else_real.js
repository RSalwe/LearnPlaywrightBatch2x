let userName = "Dev";
let passWord = "demo123";
let isAccountLocked = true;

//Logical Operator + if-else statement

if ((userName === "Dev" && passWord === "demo123") && isAccountLocked) {
    console.log("Allowed to enter");
}
else {
    console.log("Not allowed to enter");
}