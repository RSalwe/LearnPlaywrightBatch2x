//Only(Single) if is allwoed in javascript
if (true) {
    console.log("Only if is allowed");
}

//***Only (Single) else is not allowed in javascript***

else {
    console.log("Only else is Not allowed");
}