//***Only (Single) if is allowed in javascript***
if (true) {
    console.log("Only if is allowed");
}

//***Only (Single) else is NOT allowed in javascript - it will throw a syntax error***
// Below code is commented out because standalone 'else' without 'if' is invalid:
// else {
//     console.log("Only else is Not allowed");
// }
